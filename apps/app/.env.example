# apps/app environment variables (Expo)
# EXPO_PUBLIC_* are embedded at build time — do NOT put secrets here.
# Copy to apps/app/.env for local development.

EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_API_KEY=
